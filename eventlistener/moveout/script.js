const container = document.getElementById("container");

container.addEventListener("mouseout",function()
{
    alert(alert( "you left the safe zone 🙅 (mouse out event working properly)"));
});